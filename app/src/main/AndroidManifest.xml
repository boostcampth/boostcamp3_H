<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="teamh.boostcamp.myapplication">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".view.AppInitializer"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name="teamh.boostcamp.myapplication.view.password.PasswordActivity"/>
        <activity android:name="teamh.boostcamp.myapplication.view.alarm.AlarmActivity" />
        <activity android:name="teamh.boostcamp.myapplication.view.setting.SettingActivity" />
        <activity android:name="teamh.boostcamp.myapplication.view.play.PlayActivity" />
        <activity
            android:name="teamh.boostcamp.myapplication.view.main.MainActivity"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver android:name="teamh.boostcamp.myapplication.view.alarm.AlertReceiver" />
    </application>

</manifest>